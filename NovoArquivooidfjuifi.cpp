#include <iostream>
using namespace std;
main (){
	int s, tip;
		cout<<"1(x). Imposto\n";
		cout<<"2(y). Novo salario\n";
		cout<<"3(z). Classificacao\n";
		cout<<"Digite opcao desejada\n\n";
		cout<<"Digite o salario\n"; cin>>s;
		switch (tip){
			case 500>=s:
					cout<<"Imposto:"<<s*0.05;
				break;
		}
}